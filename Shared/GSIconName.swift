import Foundation

enum GSIconName: String, CaseIterable {
    case house, target, timer, barChart, user
    case clock, checkCircle, flame, play, plus
    case lock, sun, bookOpen, circleX, chartPie
    case chevronRight, star, check, bell, cloud, info, percent
}
